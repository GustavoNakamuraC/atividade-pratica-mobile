name: api_market_cap_coin
description: Aplicativo para acompanhamento de criptomoedas

version: 1.0.0+1

environment:
  sdk: '>=3.8.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.0.5
  intl: ^0.18.1
  connectivity_plus: ^4.0.2
  shared_preferences: ^2.2.2
  flutter_web_auth: ^0.5.0
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1